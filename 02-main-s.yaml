apiVersion: v1
kind: Service
metadata:
  name: nginx
  # labels: 
  #   name: nginx
  #   version: blue
spec:
  type: LoadBalancer
  selector:
    name: nginx
    version: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80