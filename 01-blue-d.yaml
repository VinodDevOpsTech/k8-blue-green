apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  labels:
    name: nginx
    version: blue
spec:
  # modify replicas according to your case
  replicas: 4
  selector:
    matchLabels:
      name: nginx
      version: blue
  template:
    metadata:
      name: nginx
      labels:
        name: nginx
        version: blue
    spec:
      containers:
      - name: nginx
        image: maxdevopstech/blue-green:1.0.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80