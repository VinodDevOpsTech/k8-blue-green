apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels: 
    name: nginx
    version: blue
spec:
  selector:
    name: nginx
    version: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80